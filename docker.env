# Значения по умолчанию для `docker compose` (без секретов).
# Для SMTP и SBIS добавьте переменные сюда или передайте через `docker compose run -e ...`.

NEXT_PUBLIC_SITE_URL=https://evoboxvl.ru
LEAD_RECEIVER_EMAIL=vl@worldcashbox.ru

SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

SBIS_ENABLED=false
SBIS_API_URL=
SBIS_API_KEY=
